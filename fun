const website = "Snapwire";
let working = "not working";

if (working !== 'working' && website === 'Snapwire');
    console.log('Riley must fix ${website}');
} else {
    console.log('Riley does not need to fix ${website}');
}
